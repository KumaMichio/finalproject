# Core dependencies
# carla is installed via .egg file — do not add here
numpy==2.2.6
opencv-python>=4.10
scipy>=1.15
scikit-learn>=1.7
PyYAML>=6.0

# PyTorch ecosystem — install separately BEFORE this file:
#   CPU:  pip install torch==2.4.1 torchvision==0.19.1
#   GPU:  pip install torch==2.4.1+cu121 torchvision==0.19.1+cu121 -f https://download.pytorch.org/whl/cu121/torch_stable.html
#   The veri-train conda env (Python 3.10) uses torch==2.4.1+cpu /
#   torchvision==0.19.1 for both the AI pipeline and YOLO11/OSNet fine-tuning.
torch==2.4.1
torchvision==0.19.1

# ML frameworks
pytorch-lightning==1.5.0
timm==0.4.12

# Computer Vision
ultralytics>=8.3.0        # YOLOv8 / YOLO11 — replaces yolov5==6.1.0
boxmot>=20.0              # ByteTrack -- v20+ uses boxmot.trackers.bbox.bytetrack.ByteTrack (new API)
pycocotools-windows
albumentations==1.1.0

# Data processing
pandas>=2.0
matplotlib>=3.10
seaborn==0.11.0

# Evaluation (Ground Truth -- MOTA/IDF1)
motmetrics>=1.4

# Utilities
tqdm==4.62.0
pillow==8.4.0
requests==2.26.0

# Development
jupyter==1.0.0
ipykernel==6.4.0

# ReID model (required for proper cross-camera matching)
# Provides OSNet pretrained on Market-1501 — much better than ImageNet ResNet50
# Install manually if this fails: git clone https://github.com/KaiyangZhou/deep-person-reid && pip install -e .
torchreid